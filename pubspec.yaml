name: tilt_action
description: Flutter package that triggers action depending on the position of the phone
version: 0.1.0
author: Timothy Itodo <itodotimothy6@gmail.com>
homepage: https://github.com/itodotimothy6/tilt_action

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sensors: ^0.4.2+2
  test: ^1.14.4

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

